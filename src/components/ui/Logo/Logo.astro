---
export interface Props {
  lang: string;
  className?: string;
}

const { lang, className = "" } = Astro.props;
---

<a href={`/${lang}`} class={className}>
  <img
    src="/img/logo.webp"
    sizes="120px"
    alt={`${import.meta.env.PUBLIC_BRAND} logo`}
    width="120"
    height="60"
    loading="eager"
    decoding="async"
  />
</a>
