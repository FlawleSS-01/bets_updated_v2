---
import ButtonsGroup from "@/components/ui/ButtonsGroup/ButtonsGroup.astro";
import { deriveFormats } from "@/lib/picture";

interface Props {
  title?: string;
  description?: string;
  items?: { title: string; description: string }[];
  imageSrc?: string;
  imageAlt?: string;
  imageTitle?: string;
  showButton?: boolean;
}

const { title = "", description = "", items = [], imageSrc, imageAlt = "", imageTitle = "", showButton = false } = Astro.props;
const big = items[0];
const rest = items.slice(1);
const fmt = imageSrc ? deriveFormats(imageSrc) : undefined;
---

{(title || items.length > 0) && (
  <section class="vblock vbento">
    <div class="vcontainer">
      <div class="vbento__head">
        {title && <h2 class="vbento__title">{title}</h2>}
        {description && <p class="vbento__desc">{description}</p>}
      </div>
      <div class="vbento__grid">
        {big && (
          <article class="vbento__big">
            <h3 class="vbento__cardTitle">{big.title}</h3>
            <p class="vbento__cardText">{big.description}</p>
            {fmt && (
              <picture>
                <source type="image/avif" srcset={fmt.avif} />
                <source type="image/webp" srcset={fmt.webp} />
                <img class="vbento__img" src={fmt.original} alt={imageAlt} title={imageTitle} loading="lazy" decoding="async" width="600" height="336" />
              </picture>
            )}
          </article>
        )}
        <div class="vbento__small">
          {rest.map((it) => (
            <article class="vbento__card">
              <h3 class="vbento__cardTitle">{it.title}</h3>
              <p class="vbento__cardText">{it.description}</p>
            </article>
          ))}
        </div>
      </div>
      {showButton && <div class="vbento__cta"><ButtonsGroup /></div>}
    </div>
  </section>
)}
